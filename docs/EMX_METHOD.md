# Method

The candidate set is frozen before tests begin. An entry requires existing
code, artifact, derivation, direct coefficient-free construction, or an
independent physical reason. It is excluded if result-selected or if it adds
fitted mechanics. Exact equivalence avoids duplicated execution while retaining
lineage; reductions declare information loss. Independence groups prevent
multiple reductions of a common upstream state from being counted as independent
support.
